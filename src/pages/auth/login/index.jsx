import Login from "@/features/auth/Login/Login";
import React from "react";

export default function LoginPage() {
  return (
    <main className="flex items-center justify-center h-screen p-4 font-instrument bg-linear-to-br from-red-600 to-red-800">
      <Login />
    </main>
  );
}
